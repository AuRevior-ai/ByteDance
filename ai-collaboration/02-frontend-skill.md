# Frontend Skill

## 前端协作约定

- 页面优先围绕 1440px 录屏场景打磨。
- 三栏工作台保持稳定，不在 Demo 前做大布局调整。
- 所有接口走 `frontend/src/services/`。
- 组件按 `chat`、`agent`、`artifact`、`preview`、`trace` 拆分。
- UI 状态优先使用 Zustand，服务端数据后续接 React Query。

